[package]
name = "movlist"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.3.1"
dotenv = "0.15.0"
serde = { version = "1.0.154", features = ["derive"] }
sqlx = { version = "0.6.2", features = ["sqlite", "runtime-actix-rustls"] }

